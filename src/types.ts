/**
 * The plugin can be configured using the following options:
 */
export interface PluginInitOptions {
  /**
   * @description
   * When true, records a customer's favorites in the customer history.
   *
   * @default 'false'
   */
  trackHistory: boolean
}
